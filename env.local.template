# Copy:  cp env.local.template .env.local
#
# Clerk (required — same app for BOTH keys):
#   https://dashboard.clerk.com → Applications → your app → API Keys
# Replace the two lines below with YOUR Publishable key and Secret key.
# Mismatched / example keys → browser error: {"code":"host_invalid","message":"Invalid host"}
#
# Local dev: use Development keys (pk_test_ / sk_test_). Do not use pk_live_ on localhost.

NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
# NEXT_PUBLIC_CLERK_FAPI=https://YOUR_INSTANCE.clerk.accounts.dev

GROQ_API_KEY=

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
